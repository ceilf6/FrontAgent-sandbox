# E-Commerce Frontend

一个基于 React + TypeScript + Tailwind CSS 的现代化电商前端应用

## 特性

- ⚡️ Vite - 极速的开发体验
- ⚛️ React 18 - 最新的 React 特性
- 🔷 TypeScript - 类型安全
- 🎨 Tailwind CSS - 实用优先的 CSS 框架
- 🐻 Zustand - 轻量级状态管理
- 🔀 React Router - 客户端路由
- 📦 严格的架构约束 - 基于 SDD 规范

## 快速开始

### 安装依赖

```bash
npm install
# 或
pnpm install
```

### 配置环境变量

```bash
cp .env.example .env
```

编辑 `.env` 文件，配置 API 地址：

```env
VITE_API_BASE_URL=http://localhost:3000/api
```

### 开发

```bash
npm run dev
```

应用将在 http://localhost:5173 启动

### 构建

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 项目结构

```
src/
├── assets/              # 静态资源
├── components/
│   ├── ui/              # UI 基础组件
│   └── features/        # 功能组件
├── hooks/               # 自定义 Hooks
├── stores/              # Zustand 状态管理
├── pages/               # 页面组件
├── api/                 # API 接口层
├── utils/               # 工具函数
└── types/               # TypeScript 类型定义
```

## 技术栈

- **框架**: React 18
- **语言**: TypeScript
- **构建工具**: Vite
- **样式**: Tailwind CSS
- **状态管理**: Zustand
- **路由**: React Router DOM
- **HTTP 客户端**: ky
- **日期处理**: date-fns
- **工具库**: es-toolkit

## 核心功能

- 商品浏览和搜索
- 商品详情查看
- 购物车管理
- 订单管理
- 响应式设计

## 架构特点

- 清晰的分层架构（UI / 业务 / 数据层解耦）
- 严格的模块边界约束
- TypeScript 严格模式
- 组件化设计
- 可维护性强

## 开发规范

项目遵循严格的 SDD（Software Design Document）规范，详细信息请查看：

- `sdd.yaml` - 软件设计文档
- `review.md` - 项目审查文档

## 许可证

MIT
