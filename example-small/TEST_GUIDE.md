# FrontAgent 测试指南 - 组件迁移场景

## 测试场景说明

这是一个**组件迁移测试项目**，用于验证 FrontAgent 的代码重构能力。

### 当前状态
- ✅ 已创建完整的电商首页
- ✅ 使用了**6个自定义组件**：Button, Card, Badge, Tag, Header, ProductCard
- ✅ 页面功能完整，包含导航、商品展示、购物车等
- ✅ 使用 Tailwind CSS 进行样式管理

### 测试目标
让 FrontAgent 根据 `sdd.yaml` 的要求，将所有自定义组件替换为 **Ant Design 组件**。

---

## 快速开始测试

### 1. 进入测试目录
```bash
cd workspace/example-small
```

### 2. 运行 FrontAgent
```bash
frontagent run "根据 SDD 要求，将所有自定义组件替换为 ant-design 组件"
```

### 3. 预期行为

#### Phase 1: 分析阶段
- [x] 列出项目目录
- [x] 读取 sdd.yaml
- [x] 读取现有组件文件
- [x] 理解迁移需求

#### Phase 2: 修改阶段
- [x] 修改 `package.json`，添加 `antd` 和 `@ant-design/icons` 依赖
- [x] 修改 `src/main.tsx`，导入 `antd/dist/reset.css`
- [x] 重构 `src/components/Header.tsx`
  - 导入改为：`import { Layout, Menu, Badge, Button } from 'antd'`
  - 组件改为使用 `Layout.Header`
  - 使用 `Menu` 组件替代导航链接
- [x] 重构 `src/components/ProductCard.tsx`
  - 导入改为：`import { Card, Tag, Button, Space } from 'antd'`
  - 使用 antd 的组件替代自定义组件
- [x] 重构 `src/pages/HomePage.tsx`
  - 移除所有自定义组件的导入
  - 从 `antd` 导入需要的组件
  - 替换所有组件使用
- [x] **删除自定义组件文件**：
  - `src/components/Button.tsx`
  - `src/components/Card.tsx`
  - `src/components/Badge.tsx`
  - `src/components/Tag.tsx`

#### Phase 3: 安装依赖
- [x] 运行 `npm install`
- [x] 确保 antd 正确安装

#### Phase 4: 验证阶段
- [x] 运行 `npm run typecheck`
- [x] 确保没有 TypeScript 类型错误

#### Phase 5: 启动验证（可选）
- [x] 运行 `npm run dev`
- [x] 在浏览器中验证功能

---

## 验收标准

### ✅ 代码变更检查

```bash
# 1. 检查 package.json 是否包含 antd
grep -A5 '"dependencies"' package.json

# 预期输出应包含：
# "antd": "^5.0.0"
# "@ant-design/icons": "^5.0.0"

# 2. 检查自定义组件是否已删除
ls -la src/components/

# 预期输出：只应该有 Header.tsx 和 ProductCard.tsx
# 不应该有：Button.tsx, Card.tsx, Badge.tsx, Tag.tsx

# 3. 检查导入语句
grep "from 'antd'" src/components/*.tsx src/pages/*.tsx

# 预期输出：所有组件都从 antd 导入

# 4. 检查 main.tsx 是否导入了 antd 样式
grep "antd/dist/reset.css" src/main.tsx

# 预期输出：import 'antd/dist/reset.css';
```

### ✅ 功能检查

启动项目后，在浏览器中验证：

1. **头部导航**
   - Logo 显示正常
   - 导航菜单可点击
   - 购物车徽章显示数字 3
   - 登录按钮样式正确

2. **Banner 区域**
   - 卡片显示正常
   - 标签显示（限时、热卖）
   - 按钮可点击

3. **分类导航**
   - 按钮组显示正常
   - 按钮样式符合 antd 风格

4. **商品列表**
   - 4个商品卡片正确展示
   - 商品图片、标题、价格显示正常
   - 标签（热销、新品等）显示正确
   - 按钮（加入购物车、收藏）可点击

5. **底部信息卡片**
   - 3个卡片并排显示
   - 卡片标题和内容正确

6. **页脚**
   - 版权信息显示

---

## 测试评分标准

| 评分项 | 分值 | 检查点 |
|--------|------|--------|
| **依赖管理** | 10分 | package.json 包含 antd 和图标库 |
| **文件删除** | 15分 | 4个自定义组件文件被删除 |
| **Header 重构** | 20分 | 使用 Layout.Header, Menu, Badge, Button |
| **ProductCard 重构** | 20分 | 使用 Card, Tag, Button, Space |
| **HomePage 重构** | 20分 | 所有组件改为 antd 组件 |
| **类型安全** | 10分 | TypeScript 编译通过 |
| **运行成功** | 5分 | npm run dev 成功启动 |
| **总分** | **100分** | |

---

## 常见问题处理

### Q1: 如果 FrontAgent 没有删除自定义组件文件怎么办？
A: 检查 sdd.yaml 中的 `files_to_delete` 配置是否正确，或者手动运行：
```bash
rm src/components/Button.tsx src/components/Card.tsx src/components/Badge.tsx src/components/Tag.tsx
```

### Q2: 如果类型检查失败怎么办？
A: 检查 antd 组件的类型定义是否正确导入，可能需要：
```bash
npm install --save-dev @types/react @types/react-dom
```

### Q3: 如果样式不正确怎么办？
A: 确保 `src/main.tsx` 中导入了 `antd/dist/reset.css`

### Q4: 如果 FrontAgent 权限问题导致无法执行命令？
A: 在命令批准提示时选择 `y` 允许执行

---

## 手动验证步骤（如果需要）

如果自动测试无法完成，可以手动执行：

```bash
# 1. 安装依赖
npm install antd @ant-design/icons

# 2. 修改 src/main.tsx
# 在第一行添加：import 'antd/dist/reset.css';

# 3. 运行类型检查
npm run typecheck

# 4. 启动项目
npm run dev

# 5. 在浏览器打开 http://localhost:5173
```

---

## 测试数据收集

请记录以下数据用于优化：

- [ ] 总执行时间：______ 秒
- [ ] 生成的步骤数：______ 个
- [ ] 成功的步骤数：______ 个
- [ ] 失败的步骤数：______ 个
- [ ] 最终得分：______ / 100分
- [ ] 主要问题：__________________
- [ ] 改进建议：__________________

---

## 下一步优化方向

基于测试结果，可以优化：

1. **SDD 描述的清晰度** - 是否需要更详细的组件映射说明
2. **错误恢复机制** - 依赖安装失败的处理
3. **代码生成质量** - 生成的代码是否符合最佳实践
4. **文件操作** - 删除、重构文件的准确性

Happy Testing! 🚀
